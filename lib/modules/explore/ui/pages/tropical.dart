import 'package:flutter/material.dart';

class Tropical extends StatelessWidget {
  const Tropical({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Tropical")
      ],
    );
  }
}