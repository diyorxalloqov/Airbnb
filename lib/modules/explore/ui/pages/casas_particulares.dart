import 'package:flutter/material.dart';

class CasasParcticulares extends StatelessWidget {
  const CasasParcticulares({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("CasasParcticulares")
      ],
    );
  }
}